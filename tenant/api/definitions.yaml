---
definitions:
  Acknowledge:
    type: object
    properties:
      message:
        type: string
        example: Success
  Tenant:
    type: object
    properties:
      username:
        type: string
        example: admin
      password:
        type: string
        example: password
      role:
        type: string
        example: role
      storage:
        type: integer
        example: 1
      memory:
        type: integer
        example: 1
      vcpu:
        type: integer
        example: 1
      groupName:
        type: string
        example: admin
  Group:
    type: object
    properties:
      name:
        type: string
        example: admin